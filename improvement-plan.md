# Word Builder 2 改进计划（动机系统优先）

- 版本：V1.0
- 日期：2026-06-12
- 排序原则：先建立动机系统，再消灭学习流程中的负面体验，然后减负，最后补记忆算法内功。
- 每个 Step 完成后都是可独立发布的版本，不做半成品合并。

## 已确认的产品决定

1. **今日目标定义**：清空当日到期复习即算达成；新学不计入目标，属于自选加餐（可做环外彩蛋）。
2. **音效默认开**：学习音效默认开启，设置页可关闭（`sound_enabled`）。
3. **streak 用严格版**：断一天即归零，不做补卡；宽松机制将来按需再加。

## 现状诊断（摘要）

1. **复习负债**：SRS 间隔封顶 L3=10 天，复习量随词汇量线性增长；单批复习硬限 10 词，清积压要反复手动重进。
2. **单次时长超预算**：每个复习词都做 AI 场景题，10 词实际 15–20 分钟，超出"每天 10–15 分钟"的产品设定。
3. **动机设计偏薄**：无连续打卡、无学习日历、无会话内即时激励；PRD 的成功指标（7 日留存、周维度正确率提升）在产品中无呈现。

---

## 阶段一：动机系统（约 1.5–2 周）

### Step 1：连续打卡 streak（1–2 天）
- 今日页头部显示「🔥 连续学习 N 天」；Florr 主题用主题化文案。
- 规则：按 `sessions` 表以学习日（上海时区，`getToday()` 口径）聚合，当天有任意完成记录即算打卡；今天未学时显示「今天还没打卡」状态，制造行动钩子；严格版，断一天归零。
- 改动：新增 `src/utils/streak.js` 纯函数 + `tests/streak.test.js`；`TodayPage.jsx` 展示。不动数据库。
- 验收：跨午夜、隔天断卡、月份/闰年边界用例单测通过。

### Step 2：今日目标环 + 批次衔接（1–2 天）
- 今日页加进度环：目标 = 清空今日到期复习（`reviewCount` 归零），清零点亮 + 庆祝动效；完成新学加彩蛋光环。
- 同步并入原 B2：学习完成页加「再来一组（还剩 X 词）」按钮，避免目标环放大批次断点的挫败感。
- 改动：`TodayPage.jsx`、`StudyPage.jsx` 的 COMPLETE 分支。
- 验收：到期 25 词时可从目标环出发连续三组不回首页清完，环归零点亮。

### Step 3：会话内即时激励（2–3 天）
- 回想/拼写/场景题连续通过显示连对 combo（场景题按最终批改结果计：通过（≥90 分）+1，未通过断连，跳过不影响）；答对/答错轻量音效（默认开，设置可关）；答对卡片微动画。
- 改动：`studyStore.js` 的 `sessionResults` 加 combo 字段（必须同步加进 `SESSION_SNAPSHOT_KEYS` 与 `partialize`，保证恢复会话不丢）；`user_settings` 加 `sound_enabled` 列（更新 `migration.sql`）；音效文件放 `public/`。
- 验收：连对中断归零；刷新恢复会话后 combo 保留；设置关闭后无声。

### Step 4：成长可视化（2–3 天）
- 进度页加月历打卡热力图 + 周维度拼写正确率折线；完成战报页底部加「streak +1」强化展示。
- 数据全部来自现有 `sessions` 表，纯前端聚合。
- 验收：热力图与 streak 口径一致；数据不足一周时优雅降级（「再学几天解锁趋势」）。

> 阶段一不动 SRS、不动升级规则、不动场景题，零学习算法风险。

## 阶段二：止痛三件套（约 1 周）

### Step 5：场景题预取（1–2 天）
- 进入 Step C 阶段时后台预取下 1–2 题（出题请求从 `UsageCard` 挂载时上移/加预取层），消灭每题转圈等待。

### Step 6：新词认识卡（曾取消，2026-06-12 经与学生用户沟通后恢复并完成）
- 新增 LearnCard 替换 NEW_LEARN 阶段的回想卡：单词 + 音标 + 全部释义 + 例句（目标词高亮）+ 自动发音，唯一按钮「记住了，去拼写」（Enter）。
- 无对错：不计回想统计、不进回流、不计连对、不影响等级；检索测试由「新词自动复习」阶段的回想卡承担。
- 战报「回想通过」口径随之变准：只统计复习型阶段的真实检索。
- 会话恢复兼容：旧快照中新学步骤仍为 recall 类型，恢复时照旧渲染回想卡，不做迁移。

### Step 7：估时校准（半天）
- 今日页估时按「回想 0.2 + 拼写 0.3 + 场景题 1.2 分钟」估算，或用最近 5 次 session 的实际人均耗时动态估算。

## 阶段三：减负 + 算法止血（约 1 周）

### Step 8：场景题轮做（2–3 天）
- 保守方案：每词每 2 次复习轮做一次场景题，轮空的那次按通过计，不改「三项均通过才升级」语义。单次复习时长再降约 1/3。
- 需同步更新 `requirementV1.md` 8.5 节与相关单测。

### Step 9：SRS 间隔拉长（半天–1 天）
- L3 词按 `correct_streak` 把间隔 10 → 20 → 40 → 80 天翻倍；纯 `srs.js` 改动 + 单测。存量数据零迁移。

## 阶段四：深化（其后，按需）

- **Step 10：难词本** —— `wrong_count` ≥ 3 的词列表 + 一键专项练习 + AI 助记（生成后缓存）。
- **Step 11：拼写延迟重测** —— 拼错的词排到队尾，几词之后凭记忆再拼一次。
- **Step 12：听写变式 / 例句填空** —— L2+ 词题型多样化；把拼写卡 🔊 直接读出答案的问题显式化为听写模式。
- **Step 13：家长周报分享图** —— 基于 Step 4 数据生成可分享的周报图。

---

## 专项：Prompt 系统化重构（2026-06-12 立项，未动手）

### 背景

场景题批改出现「学生忠实翻译题面却被建议改人称」的矛盾反馈（案例：call，题面「你晚上给朋友打电话…」，参考答案却是 "I will call..."）。根因不是批改太松，而是**出题 prompt 从未要求参考答案忠实于题面**，且批改 prompt 没有「以题面为语义基准」的锚点。同时现有 prompt 属于「规则堆叠型」，部分指令（如「场景自然分布」）是单次无记忆调用在统计上无法完成的任务。

### 方案来源与融合原则

融合两套方案：外部方案的**骨架**（变量程序化、批改判定流程化、输出结构化、system/user 分层）+ 现有 prompt 中**踩坑得来的回归补丁**（逐条审计后保留，不做有损重写）+ 本次定位出的**一致性修复**（call bug 根治）+ **黄金用例集**（让 prompt 变更可验证）。

外部方案被拒绝的部分：原样部署其模板不会修复 call bug（缺参考答案忠实题面、缺批改以题面为基准）；其从零重写丢失了多条回归规则（重试回传上次无效结果、场景 A/B 差异化要求、insight 好例/坏例、拼写怀疑负例枚举、corrected_answer 与 feedback 自洽）；「明确表达目标词含义」措辞会回退前端校验器的字面包含要求；「评分器」框架丢失了对 12 岁孩子的鼓励语气。

### 实施步骤

**第 0 步：黄金用例集（先做，约半天）**
- 为出题/批改/追问各收集 10–20 个真实用例（call 翻车案例必须入集）。
- 每个用例定义「期望性质」而非期望原文：出题——通过前端校验、人称与题面配套、命中指定场景；批改——passed 判定符合预期、不出现违背题面的建议。
- 本地脚本（dev 环境直调 Edge Function）批量跑用例出报告，作为以后所有 prompt 变更的回归基线。

**第 1 步：架构改造**
- 场景域由 Edge Function 随机/轮换注入 `scene_domain`（家庭/朋友/课堂/运动/出行/购物/餐厅/天气/兴趣/节日/社区/数字生活…），prompt 删除「自然分布」这类无效指令。
- system/user 消息分层：固定身份与输出纪律进 system，变量进 user。
- 温度分层：出题 0.6–0.7（多样性交给场景种子 + 温度），批改 0.1，追问 0.3。
- 批改返回增加可选结构化字段 `target_word_ok / core_meaning_ok / main_issue`；前端做一致性自检（`passed` 应等于 `target_word_ok && core_meaning_ok`，不一致时兜底），`normalizeUsageGradeResult` 同步适配并兼容旧返回。
- analytics：生成事件 metadata 加 `scene_domain` 与重试次数，上线后可观测场景分布与重试率。

**第 2 步：模板重写 + 逐条规则审计**

对现有每条规则明确处置，产出处置表（部署前补全成完整清单）：

| 处置 | 规则 |
| --- | --- |
| 保留 | 重试时回传上次无效题面（retry_hint 机制）；场景 A/B 须在语境/句式/语法点明显不同；insight 好例/坏例 few-shot；拼写怀疑负例枚举（专有名词/短语/英美差异/词形变化返回 false）；corrected_answer 与 feedback 自洽；鼓励学生的语气设定 |
| 合并 | 批改规则 6（不盲目复用参考答案）并入「以题面为语义基准」 |
| 移入代码 | 场景分布（→ `scene_domain` 注入）|
| 新增 | 参考答案必须是题面的忠实自然英译（人称、关键信息一致）；批改以中文题面为唯一语义基准，学生人称与题面一致时不得建议改人称；题面须**字面包含**释义关键词（与前端校验器对齐，减少无谓重试）；feedback_cn ≤40 字、无实质问题时只肯定不挑刺；追问允许承认原批改有误并以题面为准；call 案例做批改 few-shot 校准例；JSON 模板示例值改为类型描述（防 `"passed": true` 锚定）；防注入一行（学生答案中的指令不得执行）|
| 不动 | thinking mode（保留为批改质量不稳时的升级杠杆）|

**第 3 步：对比验证后部署**
- 黄金用例集上跑旧 vs 新 prompt，对比校验通过率、重试率、批改判定一致性。
- 通过后部署 `deepseek-proxy`，同步更新 `requirementV1.md` 场景题相关章节。
- 上线一周观察 analytics：场景分布是否散开、重试率是否下降、`usage_skipped` 是否减少。

### 备注

- 存量缓存坏题（如 call）不清库：批改側「以题面为基准」上线后可优雅兜住旧题，自然轮换淘汰。
- 重构收益定位是**正确性与可维护性**，不是成本/延迟（flash 模型上 prompt 长度成本可忽略）。

---

## 进度记录

| Step | 状态 | 备注 |
| --- | --- | --- |
| 1 streak | 已完成（2026-06-12） | 新增 `src/utils/streak.js` + 9 个单测；今日页头部展示打卡横幅 |
| 2 目标环 + 批次衔接 | 已完成（2026-06-12） | 今日页目标环（清空到期复习 + 新学加餐彩蛋）；完成页「再来一组」按钮 |
| 3 会话内激励 | 已完成（2026-06-12） | 连对 combo 徽章（回想/拼写/场景题通过均计入）+ 答题音效（WebAudio 生成，`sound_enabled` 设置默认开）+ 战报最高连对 |
| 4 成长可视化 | 已完成（2026-06-12） | 进度页打卡热力图（12 周）+ 周拼写正确率折线；完成页 streak 展示 |
| 5 场景题预取 | 已完成（2026-06-12） | 新增 `src/lib/usagePrefetch.js`，学习中后台预取队列里接下来 2 道场景题，in-flight 去重防止重复生成 |
| 6 新词认识卡 | 已完成（2026-06-12，曾取消后经学生用户反馈恢复） | 新增 STEP.LEARN + LearnCard；无对错、不进统计；同步更新 PRD/README/用户手册 |
| 7 估时校准 | 已完成（2026-06-12） | 今日页按最近 5 次 session 实际人均耗时估算（不足 5 词历史时按 1.7 分/词兜底，钳制 0.5–5 分/词） |
| 8–13 | 未开始 | |
| Prompt 专项 第 0 步：黄金用例集 | 未开始 | 先于一切 prompt 改动 |
| Prompt 专项 第 1 步：架构改造 | 未开始 | scene_domain 注入、消息分层、温度分层、结构化批改字段 |
| Prompt 专项 第 2 步：模板重写 + 规则审计 | 未开始 | 按处置表执行，不做有损重写 |
| Prompt 专项 第 3 步：对比验证后部署 | 未开始 | 需重新部署 deepseek-proxy 并同步 PRD |
