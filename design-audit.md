# 设计审计报告(design-review)

> 生成:2026-06-16 · 评估对象:v3.4 原始前端(线上正在跑的 indigo/Inter 版)
> 方法:gstack design-review 的评估框架(10 类清单 + AI slop 黑名单 + 设计硬规则),结合已读源码与真实截图。未跑全套浏览器自动化。
> **重要:翻这份文件的时机 = 你要加付费墙、要截图发内容的时候。不是现在。** 现在去发内容。

## 评分

- **Design Score:约 C** —— 功能扎实、无重大可用性故障,但没有设计观点,通用。
- **AI Slop Score:约 D** —— 同时命中黑名单 4 条(开发者最难自查的一项)。

## 设计系统现状(实际渲染)

- 字体:Inter 一统(display/body 不分)→ 被标记「potentially generic」。
- 主色:`#6366f1` → `#8b5cf6` 紫渐变 → **AI slop 黑名单第 1 条**。
- 图标:emoji 当功能图标(🔊🔄✨⏱️📋🔥)→ **黑名单第 7 条**。
- 圆角:16–24px 大圆角全站统一 → **黑名单第 5 条**。
- 音标:`font-family: serif`(裸默认),跨平台不一致。
- 间距:10/14/20/24 即兴值,非 4/8 scale。

## 关键发现(按 impact)

| 类别 | 发现 | impact |
|---|---|---|
| 视觉层次 | Today 三张等大 stat 卡无主次;streak/目标环被埋在中间 | 高 |
| 排版 | 单一 Inter、无模数化字阶、音标裸 serif、单词主角和按钮同字 | 高 |
| 颜色对比 | 深色 tertiary `#64748b` on `#16162a` < WCAG AA;combo 徽章与卡片顶部标签重叠 | 高 |
| AI slop | 紫渐变 + emoji 图标 + 统一大圆角 + Inter，四条同时命中 | 高 |
| 间距布局 | 非系统化 scale | 中 |
| 动效 | 登录 `float` 无限动画未裹 `prefers-reduced-motion` | 中 |
| 性能 | 字体 `@import` 阻塞;tab bar `backdrop-filter: blur(20px)` 低端机潜在卡顿 | 中 |
| 交互状态 | `focus-visible` 有(加分);emoji 非真图标、跨平台漂移 | 中 |

## 能实现什么(建议清单,标到文件)

**高(动一下观感立刻不一样):**
1. 主色脱离 indigo→violet 默认 —— `src/index.css` 的 `--primary` 系令牌。
2. 功能 emoji 换一致 SVG 图标集 —— 消除 🔊 违和。
3. 深色 tertiary 文字提对比过 AA —— `src/index.css` `--text-tertiary`。
4. combo 徽章重叠修复 —— `src/pages/StudyPage.jsx` + 卡片层级。

**中(系统性提质):**
5. 给单词主角引入 display 字体。
6. 间距/圆角收敛成刻度。
7. 音标换字体;登录动画裹 reduced-motion。

**打磨:**
8. Today 首屏重构(目标环/streak 当主角)。
9. L0-L3 稀有度做成色彩主线。

## 和 strategy.md 的对齐(关键)

「前端视觉重做」已在 strategy.md 否决(对前 10 单零贡献)。这份审计不推翻它,按战略切两半:

- **现在就忽略**:第 5、6、7、8、9 条。让产品更美,但对「陌生人付不付费」几乎零影响。
- **值得在「加付费墙」那一步顺手做**(和转化/可信度相关):第 1、2、3、4 条。原因:你要发小红书、要陌生人掏钱,**产品截图的第一眼观感有转化价值**。还在用「紫渐变 + 3D emoji」的截图会让人下意识觉得「随手玩具」;换主色、换正经图标、修徽章重叠,成本几小时,却能让内容配图和付费墙页面「像个正经产品」。这不是为了美,是为了卖得出去。

**一句话:90% 现在别做,只有「让产品截图看起来可信」那 4 条,等你做付费墙/截图发内容时顺手做。**
